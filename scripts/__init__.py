"""Companion scripts for 'Why (Voice) AI Agents Fail'.

Each `0N_*.py` file is a self-contained, runnable demonstration of a
production voice-agent failure mode and the architectural fix.

Shared utilities live in `_utils.py` so the scripts stay focused on
the failure mode being demonstrated rather than credential plumbing.
"""
